# 0.5.2 需求与审核记录

本轮按用户要求由三个独立审核员只读检查。每条修改都绑定需求；已完成项必须有代码或运行证据。超出需求、相互冲突或依据不足的意见先向用户确认，不自动扩大实施范围。

| 需求 | 实现 | 审核与证据 |
| --- | --- | --- |
| 按给定分镜重做呼吸灯 | 蓝紫粉橙矢量边缘、四角高光、2 秒周期、状态点同步、焦点优先 | 审核员 1；`scripts/visual-smoke.mjs` 检查实际亮度变化与相同起始时刻，截取峰值和低谷 |
| 提升整个界面的清晰度 | 提高信息字号、加深阅读层、保护 ANSI 色彩与用户终端字号 | 审核员 2；三主题、820×560 窄窗、3840×2160 高 DPI 与资源管理器截图 |
| 删除无用图层和冗余代码以减少体积 | 删除旧灯条、旧整卡动画、反光伪层、废弃侧栏样式及未使用导入；禁止未使用 TS 代码；包中排除映射、构建文件、重复和其他平台 PTY、Python 缓存 | 审核员 3；包文件检查、前后大小比较及真实 PTY 启动 |
| 保留原有操作约束 | 完成最多 3 次呼吸；空闲不重新提醒；手动完成常绿；只通过标题/展开入口放大，小窗口继续输入 | `visual-smoke`、`smoke`、`completion-smoke`、`focus-motion-smoke` |

## 审核意见处理

| 意见 | 需求依据 | 处理 |
| --- | --- | --- |
| 旧 2.8 秒整卡动画与新 2 秒动画重复 | 同步呼吸 + 删除冗余 | 删除旧动画，测试改为检测边缘与状态点 |
| 初版只有两个角高光 | 参考图“四角高光” | 在同一渐变中补齐四角，无新增覆盖层 |
| 部分操作信息仍为 9px，森林控制层较透明 | 整体清晰度 | 关键标签至少 11px，加深控制层；窄卡片与三主题截图复核 |
| integration 混入 Python 缓存及 C# 构建源码 | 删除无用文件减少体积 | 添加打包过滤；保留实际运行的 EXE、JS、Shell、Python 文件 |
| 校验应检查 file-clipboard.exe，不只检查源码 | 清理不能破坏现有文件操作 | 校验实际运行文件，配合打包桌面回归 |

所有意见均有需求依据；没有需要用户补充确认的设计扩展。原油画背景未重绘，不宣称升级为 4K 素材。

## 验证记录

初次打包视觉验证已通过：实际运行时边缘透明度 0.307–0.850，边缘与状态点起始时刻相同、周期均 2000ms；完成后停止，主题切换及重复完成记录不会重启旧提示。六个窄卡片控件均在标题栏范围内，小窗口输入保持原位。

三个审核员均已复审通过，前述五条意见全部关闭。

| 本地校验 | 结果 |
| --- | --- |
| TypeScript 严格/未使用代码检查、生产构建 | 通过 |
| 单元与集成测试 | 84 项：83 通过，1 项 Linux 专用测试在 Windows 跳过 |
| 打包版视觉测试 | 通过；含实际连续动画、完成停止、减少动态效果、三主题与高 DPI |
| 打包版完整桌面测试 | 通过；真实 ConPTY/Codex 启动、拖动、链接、图片/视频/HTML、大文件与完成状态 |
| 打包版文件操作与语音界面测试 | 通过；本机文件复制、粘贴、创建、重命名、删除及隔离语音夹具 |
| 实际 NSIS 安装器内容的展开/返回动画 | 通过；含 Windows 减少动态效果设置、Shift+Enter、草稿和终端节点保留 |
| 安装器校验与运行文件检查 | 通过；SHA-512、更新信息、7 档图标、PTY/SSH/剪贴板运行文件及排除规则 |

| 体积项目（bytes） | 0.5.1 | 0.5.2 |
| --- | ---: | ---: |
| 安装目录 | 400,770,986 | 392,602,137 |
| app.asar | 9,240,876 | 5,497,927 |
| 解包的原生模块 | 6,025,362 | 1,660,863 |
| 前端 dist | 6,251,928 | 3,898,934 |

相同本地构建环境中，安装目录减少 **8,168,849 bytes（约 8.17 MB，2.04%）**。这不是安装器下载大小的降幅；Electron 基础运行库仍占主要体积。0.5.2 本地安装器大小为 115,490,076 bytes。

复现：`npm run dist`，然后串行运行 `node scripts/verify-update-artifacts.cjs`、`npm run test:visual -- --packaged`、`npm run test:desktop -- --packaged`、`npm run test:workspace -- --packaged --slow-copy`。安装器动画按 GitHub Actions 中的 payload 提取步骤执行；发布流程还会运行 SSH、恢复、分屏和 Markdown 等完整回归。
