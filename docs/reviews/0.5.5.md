# 0.5.5 需求与审核记录

| 用户指令/确认 | 修改 | 验证 |
| --- | --- | --- |
| 截图中 Codex 输入黑条改透明 | 根据真实 TUI 采样，将活动 Codex 分屏中的 #1e1e1e 背景透明化；包含输入留白和换行区域 | 独立 CODEX_HOME 启动真实 Codex，检查空输入/多行/清空/缩放及三主题 |
| “字体可以增亮” | 原先 DIM 字形 alpha 0.9→1，白色提示达到完整亮度；保持 RGB 色相、600/700 字重和用户字号 | 真实占位字与 ANSI/256色/真彩色/反色采样 |
| 范围确认：“统一去掉 Codex 同类黑底” | 输入与历史指令同色背景一起处理；按每个分屏自身的 codexActive 生效 | 认证生命周期输出样例、同项目普通 shell 对照；退出 Codex 后普通终端背景恢复原语义 |
| 既有小卡片交互和清晰选区 | 排除选择装饰层和块光标，保留红绿 diff、ANSI 与默认反色背景 | 中文粘贴、Shift+Enter、复制选区、放大/返回、字号变化、会话和草稿保持 |

审核员最初建议只处理当前输入且保留同色历史背景；由于仅凭 xterm 最终颜色无法区分来源，先向用户确认，得到统一透明处理的明确选择后再实现。没有全局清空 ANSI 背景，没有增加扫描循环、观察器、图层、依赖或改写 PTY 数据。

源版证据：`composer-1789956645372`，真实 Codex 0.155.1 空闲 TUI 三主题截图、多行输入、选区及放大截图；本地隔离 provider 没有收到模型 POST，测试和终端清理正常退出。旧版本/功能列表 smoke 不作为输入透明的证据。

打包证据：`composer-1789957213287` 在真实 Codex 中通过空白占位、三主题、中文多行粘贴、Shift+Enter、蓝色选区、17px 字号及放大返回、清空后占位恢复；会话与终端 DOM 身份不变，无模型 POST，进程正常退出。`material-1789957291006` 通过三主题 DIM alpha=255、ANSI/256色/真彩色和反色选区恢复，以及实色回退。

TypeScript/Vite、Windows 安装包/便携包、更新元数据/图标/精简包验证通过。单元与集成检查 88 通过，1 项 Linux 专用检查在本机跳过，远端 Linux 作业继续执行。

审核员提出的测试资源收尾问题已修正：全格式剪贴板仅在内存备份、测试仍拥有当前内容才恢复，HTTP 模拟服务在最外层 finally 关闭，测试任务先结束再退出独立终端。最终测试额外等待第一行粘贴呈现并断言第二行紧随其后，证据 `composer-1789957727169`；进程正常退出。

三位审核员独立只读复审全部 PASS：需求覆盖、可见行为、技术与测试资源边界均无剩余必修项，用户确认的范围已与修改及验证绑定。
