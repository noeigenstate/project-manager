# 0.5.6 需求与审核记录

视觉方向：macOS 山川与海岸摄影作为整窗背景，保持现有玻璃材质与亮白文字；主题预览和主背景共用素材，切换不增加动画或终端重启。

| 用户指令/已确认约束 | 修改 | 验证 |
| --- | --- | --- |
| “主题背景可以网上找苹果壁纸”，确认选择 macOS 山川、海岸风景 | 从 512 Pixels macOS 档案选取 High Sierra、Big Sur Day、Sierra，来源及处理方式记录在 background-art.md | 来源核对、原图及应用实图检查 |
| 现有主题选择和配色 | 保留 forest/mountain-blue/wild-red 与名称，仅更新壁纸/说明/预览 | 切换、保存、重启恢复、同一终端与草稿保留 |
| 高清、透明界面中文字清楚 | 等比 3840px WebP，原玻璃染色/字体/ANSI/透明输入不变 | 宽窗/窄窗/高 DPI、真实 Codex 与材质检查 |
| 体积与简洁 | 三图合计 7.18 MB；不复制缩略图，不增加网络下载/图层/循环/依赖；移除旧 SVG，旧 PNG 移至测试目录 | 打包资源、离线加载、源引用检查 |

预审提示已落实：主背景与缩略图同步；Big Sur 的中心偏上裁切在两处一致；不增加新主题 ID 或设置结构；原主题状态无需迁移。

GUI 验证使用独立 PROJECT_GRID_DATA_DIR 且串行执行，未操作用户真实会话。

- 源主题检查 `themes-markdown-1789966818016` 和最终包 `themes-markdown-1789967113887`：三个背景/缩略图均在断网状态成功解码，宽度3840，复用本地资源和同一裁切位置；主题保存、重启恢复、终端身份/草稿/ANSI、本地和SSH Markdown正常。
- 最终包 `visual-1789967480052`：820px窄窗、3840×2160高DPI截图、全屏/返回、同步呼吸与安静完成状态通过。Windows 150%缩放下底边为561.333 CSS px，而innerHeight取整为561；两处旧精确相等测试改为小于1px的舍入容差，产品布局未改。
- 最终包 `composer-1789967370173`：真实Codex透明输入区、亮字、中文多行、复制粘贴、字号/放大返回及草稿保持通过，无模型提交。
- 最终包 `material-1789967575307`：6/16卡片与6个真实输出终端、三主题ANSI/选区/反色及减少透明度回退通过。
- TypeScript/Vite、Windows打包及更新元数据/图标/精简包验证通过；单元与集成测试88通过，1项Linux专用检查在本机跳过。
- ASAR恰有3张壁纸，每张与源文件一致，并包含第三方归属说明；旧背景、测试PNG和源码映射不在包内。安装器119,690,188 bytes，较本地0.5.5增加4,199,905 bytes；安装目录396,784,441 bytes。本轮提升素材质量，未宣称缩小应用。
- README总览图同步为本次实际打包界面。整体桌面回归 `desktop-1789967728091` 通过，包括六终端、拖动、旧PNG测试图片、HTML/视频/大文本、文件链接、侧栏、小窗输入及空闲完成去重。
- 三位审核员独立只读最终复审全部PASS，需求覆盖、可见行为及包体/资源检查没有剩余必修项。
